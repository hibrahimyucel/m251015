import { projectUnit, dictionary } from "./projectTypes";
import { adminPanel, dashboard, logIn, logo, project } from "./projectUnits";

export const info: dictionary<projectUnit> = {
  project,
  logIn,
  dashboard,
  logo,
  adminPanel,
};
